[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "topx"
version = "1.0.0"
description = ""
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
